import React from 'react';
import styled from 'styled-components';

const StyledHeroContainer = styled.section`
  display: grid;
  gid-template-rows: minmax(auto, )
`;

const heroContainer = (props) => {

  return (

  );
}

export default heroContainer;